{
  "name": "arkham-hunt-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:e2e": "npm run build && playwright test e2e/game-loop.spec.js e2e/service-worker.spec.js e2e/resize-text.spec.js --config=playwright.config.js",
    "screenshots": "npm run build && playwright test e2e/readme-screenshots.spec.js --config=playwright.config.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@sentry/browser": "^10.0.0",
    "preact": "^10.22.0",
    "preact-router": "^4.1.2",
    "uqr": "^0.1.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@preact/preset-vite": "^2.8.4",
    "@testing-library/preact": "^3.2.4",
    "jsdom": "^25.0.1",
    "vite": "^8.3.0",
    "vitest": "^5.0.0"
  }
}
